apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cnpg-demo
spec:
  enablePDB: false
  instances: 1
  imageCatalogRef:
    kind: ImageCatalog
    major: 17
    name: postgresql
    apiGroup: postgresql.cnpg.io
  resources:
    requests:
      cpu: "0.5"
    limits:
      memory: "1024Mi"
  storage:
    size: 50Gi
    storageClass: ionos-enterprise-ssd