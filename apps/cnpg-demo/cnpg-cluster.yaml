apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cnpg-demo
spec:
  enablePDB: false
  instances: 1
  imageCatalogRef:
    kind: ImageCatalog
    major: 17
    name: postgresql
    apiGroup: postgresql.cnpg.io
  resources:
    requests:
      cpu: 1
      memory: 1Gi
    limits:
      memory: 1Gi
  storage:
    size: 50Gi
    storageClass: ionos-enterprise-ssd